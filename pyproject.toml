[project]
name = 'resow'
description = 'resow project'
version = '1.0'
readme = 'README.md'
dependencies = ['ee', 'numpy', 'osgeo']

[build-system]
build-backend = 'flit_core.buildapi'
requires = ['flit_core >=3.2,<4']